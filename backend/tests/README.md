# Тестирование бэкенда

## Структура тестов

```
tests/
├── global-setup.ts          # Глобальная настройка тестовой БД (Docker)
├── helpers.ts               # Вспомогательные функции для тестов
├── setup.ts                 # Очистка БД перед каждым тестом
├── README.md                 # Эта документация
├── unit/                     # Unit-тесты (без БД)
│   ├── controllers-validation.test.ts
│   ├── services-utils.test.ts
│   └── middleware.test.ts
├── integration/              # Интеграционные тесты (с БД)
│   ├── auth-user-routes.test.ts
│   └── trainer-nutrition-progress-routes.test.ts
├── e2e/                      # E2E-тесты полного цикла
│   ├── auth-invites-nutrition.test.ts
│   └── progress-chat-notifications.test.ts
├── invites-trainer-client/   # Тесты приглашений тренер-клиент
│   ├── accept-invite.test.ts
│   ├── duplicate-invite.test.ts
│   ├── favorites.test.ts
│   ├── reject-invite.test.ts
│   ├── send-invite.test.ts
│   └── clients-list.test.ts
├── nutrition/                # Тесты функционала питания
└── progress/                 # Тесты функционала прогресса
```

### Типы тестов

- **`unit/`** - Unit-тесты для отдельных функций, сервисов, middleware (без подключения к БД)
- **`integration/`** - Тесты API роутов и контроллеров с реальной БД
- **`e2e/`** - End-to-end тесты полного пользовательского цикла с запуском сервера
- **`invites-trainer-client/`** - Специализированные тесты логики приглашений между тренерами и клиентами
- **`nutrition/`** - Тесты функционала планов питания
- **`progress/`** - Тесты отслеживания прогресса клиентов

## Настройка

1. **Docker** - требуется для запуска тестовой PostgreSQL базы данных
2. **`.env.test`** - создайте на основе `.env.example` с настройками тестовой БД
3. **Автоматическая настройка** - `global-setup.ts` автоматически:
   - Запускает Docker контейнер с PostgreSQL
   - Ждет готовности БД
   - Применяет Prisma миграции
   - Останавливает контейнер после тестов

## Запуск тестов

```bash
# Все тесты
npm test

# По типам
npm run test:unit        # Unit-тесты
npm run test:integration # Интеграционные тесты
npm run test:e2e         # E2E-тесты

# Отдельные файлы
npx vitest run tests/unit/
npx vitest run tests/integration/
npx vitest run tests/e2e/
npx vitest run tests/invites-trainer-client/
```

## Особенности реализации

- **Vitest** - тестовый фреймворк с последовательным выполнением (`maxWorkers: 1`, `sequence: { concurrent: false }`)
- **Fastify** - тестовое приложение создается через `buildApp()` из `app.ts`
- **Prisma** - ORM для работы с тестовой БД
- **Изоляция** - каждый тест начинается с чистой БД благодаря `setup.ts`
- **Хелперы** - `helpers.ts` предоставляет функции для создания тестовых данных и авторизации

## Вспомогательные файлы

- **`global-setup.ts`** - управление жизненным циклом тестовой БД через Docker
- **`setup.ts`** - очистка всех таблиц перед каждым тестом
- **`helpers.ts`** - утилиты для создания пользователей, авторизации, тестового приложения

## Статус тестов

✅ **Реализованы и проходят:**

- E2E: Полный цикл аутентификации, приглашений, питания, прогресса, чатов, уведомлений
- Integration: Роуты аутентификации и пользователей
- Unit: Сервисы токенов, middleware, валидация контроллеров
- Invites: Принятие/отклонение приглашений, избранные клиенты, дубликаты

🔄 **В разработке:**

- Nutrition: Полные тесты планов питания
- Progress: Расширенные тесты прогресса

## Примечания

- Тесты используют последовательное выполнение для стабильности работы с БД
- Socket.IO замокан в тестовом приложении для предотвращения ошибок уведомлений
- Supabase функции отключены в тестовой среде
- Все тесты используют уникальные email/timestamp для избежания конфликтов
